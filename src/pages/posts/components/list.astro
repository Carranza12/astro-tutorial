---
let { order, list } = Astro.props;
list = list ? list : []
---

{
  order === true && (
    <ol class="post-list">
      {list.map((listItem) => (
        <li set:html={listItem.content} />
      ))}
    </ol>
  )
}
{
  order === false && (
    <ul class="post-list">
      {list.map((listItem) => (
        <li set:html={listItem.content} />
      ))}
    </ul>
  )
}
